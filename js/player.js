class Player {
    constructor() {
        this.x = 300;
        this.y = 350;
        this.width = 70;
        this.height = 150;
        this.color = "blue";
    }

    // let gravity = 350
    // let playerFall = function(){}

    // for (let i = gravity; i > 0; i--)) {
    //     if (placeFree(player.x, player.y + i) {
    //        player.y += i;
    //        break;
    //     }
    //  }

    jumpPlayer() {
        this.y = this.y - 150;
        // if (this.y < 350)
    } 
}